package edu.aua.cryptography.hash.impl;

import edu.aua.cryptography.hash.core.Hash;

public class Sha256Hash implements Hash {
    @Override
    public byte[] apply(byte[] message) {
        return new byte[0];
    }
}
